on: [push]
jobs:
  build:
    runs-on: alpine-3.22
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Test step
        run: echo "This is a test step to verify the workflow is running correctly"
